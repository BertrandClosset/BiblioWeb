{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Édition du Livre</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('partieA_livre_liste') }}">
            Retour à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
